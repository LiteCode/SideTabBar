import XCTest
@testable import SideTabBar

final class SideTabBarTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SideTabBar().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
